[phases.setup]
nixPkgs = ['nodejs']

[phases.install]
cmds = ['pwd', 'ls ./src/mastra -la', 'yarn install']

[phases.build]
cmds = ['pwd', 'ls -la', 'yarn mastra build']

[start]
cmd = 'pwd && ls -la && yarn start'

[variables]
NODE_ENV = 'production'
